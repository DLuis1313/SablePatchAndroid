# Este diretório conterá o libsable_rapier.so gerado pelo GitHub Actions (build.yml).
# NÃO faça commit do .so diretamente – ele é gerado automaticamente na CI.
# Se quiser compilar localmente, use: scripts/build_rapier_android.sh
